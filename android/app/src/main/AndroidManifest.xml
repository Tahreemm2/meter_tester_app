<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Required for all build variants (debug/profile/release) to talk to
         the MEPCO backend API. The debug-only manifest's INTERNET permission
         is for the Flutter tool's own debugging channel and does NOT cover
         release/profile builds — this entry is what the app itself needs. -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- GPS coordinates are mandatory on every inspection submission
         (backend/api/data.php validates gps.latitude/longitude). Coarse is
         requested as a fallback if fine location is denied but coarse is
         granted; the app still requires fine-level accuracy to pass the
         backend's GPS_MAX_ACCURACY_METERS check in practice. -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>

    <!-- Camera capture for the 2-12 required geo-tagged inspection photos. -->
    <uses-permission android:name="android.permission.CAMERA"/>

    <!-- Selecting existing photos from the gallery as an alternative to the
         camera (Android 13+ scoped media permission; image_picker falls
         back to READ_EXTERNAL_STORAGE on older API levels automatically). -->
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"/>

    <application
        android:label="mnt_module2"
        android:name="${applicationName}"
        android:icon="@mipmap/ic_launcher">
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:taskAffinity=""
            android:theme="@style/LaunchTheme"
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|smallestScreenSize|locale|layoutDirection|fontScale|screenLayout|density|uiMode"
            android:hardwareAccelerated="true"
            android:windowSoftInputMode="adjustResize">
            <!-- Specifies an Android theme to apply to this Activity as soon as
                 the Android process has started. This theme is visible to the user
                 while the Flutter UI initializes. After that, this theme continues
                 to determine the Window background behind the Flutter UI. -->
            <meta-data
              android:name="io.flutter.embedding.android.NormalTheme"
              android:resource="@style/NormalTheme"
              />
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <!-- Don't delete the meta-data below.
             This is used by the Flutter tool to generate GeneratedPluginRegistrant.java -->
        <meta-data
            android:name="flutterEmbedding"
            android:value="2" />
    </application>
    <!-- Required to query activities that can process text, see:
         https://developer.android.com/training/package-visibility and
         https://developer.android.com/reference/android/content/Intent#ACTION_PROCESS_TEXT.

         In particular, this is used by the Flutter engine in io.flutter.plugin.text.ProcessTextPlugin. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.PROCESS_TEXT"/>
            <data android:mimeType="text/plain"/>
        </intent>
    </queries>
</manifest>
